You can use the [DayCellTemplate](https://docs.devexpress.com/Blazor/DevExpress.Blazor.DxDateRangePicker-1.DayCellTemplate) property to highlight individual dates in the Date Range Picker's calendar. The template's `context` parameter allows you to access the current date-time object and associated settings.

The code used above applies different styles to different groups of dates.