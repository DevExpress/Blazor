<DemoPageSectionComponent Id="DialogsAndWindows-MessageBox-Overview" ShowSizeMode="true">
    <ChildContentWithParameters Context="Params">
        <div class="d-flex flex-column align-items-center justify-content-center p-3">
            <DxButton RenderStyle="ButtonRenderStyle.Secondary" Click="() => MessageBoxVisible = true">CLICK TO SHOW A MESSAGE BOX</DxButton>
            <p>RETURN VALUE: @(Result is not null ? Result.ToString().ToUpper() : "NO VALUE")</p>
        </div>

        <DxMessageBox @bind-Visible="MessageBoxVisible"
                      Title="Cannot open the file"
                      Text="The file may have been moved, renamed, or deleted."
                      OkButtonText="Show details..."
                      CancelButtonText="OK"
                      CloseOnOutsideClick="true"
                      RenderStyle="@MessageBoxRenderStyle.Warning"
                      SizeMode="@Params.SizeMode"
                      Type="@MessageBoxType.Confirmation"
                      Closed="@Closed">
        </DxMessageBox>

    </ChildContentWithParameters>

    @code {
        bool MessageBoxVisible { get; set; } = false;
        private bool? Result { get; set; } = null;
        
        void Closed(bool Confirmed) {
            Result = Confirmed;
        }
    }

</DemoPageSectionComponent>

